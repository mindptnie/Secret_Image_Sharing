model_params:
  in_channels: 3
  latent_dim: 10
  manual_seed: 67

data_params:
  data_path: "Data/"
  train_batch_size: 64
  val_batch_size:  64
  patch_size: 64
  num_workers: 4

exp_params:
  LR: 0.001
  weight_decay: 0.000001
  scheduler_gamma: 0.5
  kld_weight: 0.001

trainer_params:
  gpus: [1]
  max_epochs: 10

logging_params:
  save_dir: "logs/"
